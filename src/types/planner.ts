export interface planner {
    planner_id: number
    created_at: string
    start_date: Date
    end_date: Date
    title: string
    user_id: string
    confidence: string
    distance: string
    festival_id: number
}